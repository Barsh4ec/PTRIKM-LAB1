- name: Deploy Containerized App
  hosts: all
  tasks:
  - name: Install Docker
    apt:
      name: docker.io
      state: present
      update_cache: yes
  - name: Run Application Container
    docker_container:
      name: my-app
      image: nginx:latest
      ports:
        - "80:80"